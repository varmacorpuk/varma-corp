[project]
name = "varma-corp"
version = "0.1.0"
description = "Varma Corp. company kernel — Board Member control room"
requires-python = ">=3.12"
readme = "README.md"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

[tool.setuptools.packages.find]
include = ["varma*"]
